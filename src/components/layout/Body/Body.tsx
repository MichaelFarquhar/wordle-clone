import React from 'react';
import Keyboard from '../../Keyboard/Keyboard';
import './Body.scss';

export const Body = () => {
    return (
        <main className="main">
            <Keyboard />
        </main>
    );
};
